<%= form_with(model: chinese_food, local: true) do |form| %>
  <% if chinese_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chinese_food.errors.count, "error") %> prohibited this chinese_food from being saved:</h2>

      <ul>
        <% chinese_food.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
